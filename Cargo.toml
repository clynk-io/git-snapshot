[package]
edition = "2021"
name = "git-snapshot"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
git-repository = {version = "0.18.0", features = ["default", "unstable"]}
git2 = "0.14"
thiserror = "1.0"
